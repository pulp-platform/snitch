  ${mst_dir} ${bus.addr_type()} ${prefix}_paddr,
  ${mst_dir} apb_pkg::prot_t    ${prefix}_pprot,
  ${mst_dir} logic              ${prefix}_psel,
  ${mst_dir} logic              ${prefix}_penable,
  ${mst_dir} logic              ${prefix}_pwrite,
  ${mst_dir} ${bus.data_type()} ${prefix}_pwdata,
  ${mst_dir} ${bus.strb_type()} ${prefix}_pstrb,
  ${slv_dir} logic              ${prefix}_pready,
  ${slv_dir} ${bus.data_type()} ${prefix}_prdata,
  ${slv_dir} logic              ${prefix}_pslverr